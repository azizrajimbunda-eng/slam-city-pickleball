# SLAM CITY — 8-Bit Pickleball

Broadcast-view doubles pickleball, 8-bit canvas style, authentic rules. Single-file game.

## Links
- **Live (play here / test on phone):** https://azizrajimbunda-eng.github.io/slam-city-pickleball/
- **Repo:** https://github.com/azizrajimbunda-eng/slam-city-pickleball (Pages auto-deploys `main`, ~30-60s)
- **Artifact mirror:** https://claude.ai/code/artifact/3f224f7d-95c5-4b67-b2c1-fee2fe3ff3ea — update by publishing `index.html` with the Artifact tool passing this URL as `url`; favicon 🏓.
- Dev server: `.claude/launch.json` config `slam-city` → localhost:8642 (never run servers via Bash).

## Architecture (deliberate decisions — don't relitigate without cause)
- **Single-file `index.html`** (~1300 lines): the artifact host requires a self-contained file, and no build step keeps deploys trivial. Split only if scope doubles again.
- **The sim is true 3D** (`cx, cy, cz` + gravity `GZ`, net height `NET_H`), and since 2026-07-15 the renderer is a **true perspective camera** (`proj()` = pinhole camera `CAM` behind the near baseline + `camPanX` ball-follow pan; wall/crowd stay screen-space for parallax). The whole game draws through `proj()`'s `{sx, sy, gy, sc}` contract — camera changes never touch game logic. Camera constants (`CAM.back`/`CAM.f`) were tuned via headless sweep over every legal position: re-run that sweep (zero off-canvas overflow) after any camera change. `WHGT 0.42` keeps ball arcs at the old broadcast proportions — dinks read flat, raising it makes every arc taller.
- **One tuned pace** (`TUNE`, pace 0.45): patient dink base; `paceBoost` surges toward `FAST_PACE` on drives/smashes/hand battles, then decays. No difficulty modes (removed by user request).
- **Virtual input layer**: keyboard `keys{}` + joystick `touchVec` merge in `applyHumanMovement()`; all swings go through `doSwing(type)`. Gamepad API would be a ~30-line add here.
- **Phase state machine** in `update()`: `serve`, `serveMeter` (human serve timing), `play`, `dead` (settle beat + result banner).
- **`body.mobile` app mode** (set when touch detected): full-bleed canvas, no page chrome, deck hidden behind ⚙ menu, controls sized via CSS vars on `.padDeck` (`--tb` button diameter, `--pad` = 2.6×tb diamond box, `--sz/--sb/--sn` stick). Portrait-first; landscape overlays controls on the side bars. Desktop page untouched — everything scoped to `body.mobile`.
- Diamond button anchors are **19.5%/80.5% of a 2.6×tb box** = exactly tb/2 from the edge. Off-screen bleed happened twice before; verify `getBoundingClientRect` vs viewport after any control-layout change.

## Game systems (where things live in index.html)
- Rules: side-out scoring + 3-number call (`scoreCall()`, 0-0-2 opening) in `scorePoint()`/`startPoint()`; two-bounce + kitchen (NVZ) faults enforced in `stepBall()` hit checks; ball may bounce once on any shot (dink battles play off the bounce).
- Shots: `launch(fromTeam, type, quality, aim)` — types serve/return/drop/dink/drive/smash/lob/atp. Human aim = direction held at contact. `bestShot(cz, cy)` recommends what the incoming ball calls for (bracket text + button glow + hint bar); wrong choice costs accuracy (TOO LOW!/NET RISK!/SITTER!).
- Specials: ATP (wide ball + wide player; `ball.atp` skips the net-cord check), Erne (volley from `|cx|>0.85` beside the kitchen is legal), speed-up hand battles (`handBattle` frames pin pace high, `hbCount` ramps AI miss pressure).
- Announcer: `say()` via speechSynthesis — only speaks in `controlMode` (guarantees user gesture, keeps attract mode quiet); utterances queue (never cancel-then-speak: drops lines on iOS) and are ref-held in `curUtter` (Chrome GC bug).
- Fullscreen: `goFullscreen()` + `autoFullscreen()` on first control touch, skipped when installed standalone. PWA: `manifest.json` + `sw.js` (network-first, cache fallback — cache-first previously served stale builds; bump `CACHE` version on SW changes).

## Verification (do this before every commit)
1. `node tests/rules-harness.js` and `node tests/rules-harness.js human` — headless full games asserting rule invariants (side-out scoring, rotation, two-bounce, kitchen) with exact contact-time checks via a wrapped `doReturn`. Both must print `ALL RULE INVARIANTS HELD`.
2. `node tests/positioning-harness.js` — must print `ALL POSITIONING INVARIANTS HELD`. **Rule-correct ≠ well-positioned: the rules harness passed before AND after all three positioning bugs, so it structurally cannot cover this.** Asserts line presence >50%, dead-zone (1-3ft) <25%, no hitting-team depth bucket in the 3-15ft transition zone >10%, defender spread p10 >3ft. Validated to actually catch: run it against `git show feb0ec5:index.html` (pre-fix) and it produces 10 violations covering all three bugs; exits 1 on failure, 0 on pass. Non-flaky (5/5, line 76.6-83.1% vs the 50% threshold). Only checks the *hitting* team for transition parks — the receiving team legitimately sits deep during the two-bounce phase (~12ft).
3. Browser pane: **RAF is throttled to zero when the pane is hidden** (`document.hidden`) — a frozen canvas is NOT a game bug. Initial frames still render for screenshots. For mobile layout checks: force `document.body.classList.add('mobile')` + `padDeck.classList.add('on')` (the pane reports a fine pointer), resize to 375×812 / 812×375, assert rects inside viewport + no scroll.
4. Harness DOM stubs live in `tests/rules-harness.js` and are duplicated in `tests/positioning-harness.js` — new `document.*` usage in the game needs a matching stub in BOTH.
5. Ship: commit → `git push` → poll `curl` the Pages URL for a new marker string → redeploy artifact.

## Ways of working (user preferences, established over the project)
- Filipino/English casual tone ("bro" is fine). User playtests on iPhone and gives feel-based feedback; translate it into mechanics/UX root causes and confirm the diagnosis with AskUserQuestion when ambiguous.
- Resource efficiency matters: work inline (whole file fits in context); no subagent fan-outs or workflows for routine changes. Sonnet is fine for CSS/layout stages; verification fan-outs at Haiku/low if ever needed; `/code-review` low–medium after big milestones. A 48-agent ultracode review was tried once — overkill; findings were adjudicated inline instead.
- Commit every milestone with a detailed message; the git log is the project history.

## Current state / open threads (2026-07-15)
- **Shared publicly for indie-dev playtesting.** `README.md` shipped (what it is, live link, controls, known rough edges); feedback channel is GitHub Issues. Repo is public — the play link, not the repo link, is what to hand to testers. Not yet cross-posted to itch.io / r/IndieDev (open idea for reach).
- **Playtest feedback is now arriving from friends, not just the two original testers.** Treat their feel-reports the same way: translate to a mechanics/UX root cause, *measure it headless before believing your first hypothesis* (see kitchen-line entry below).
- **AI now holds the kitchen line** (shipped 2026-07-15). Report was "ayaw pa rin pumasok sa kitchen line kahit may opportunity". First hypothesis — AI structurally locked out — was **wrong**; it reached the line in 17/29 points and even stepped inside. Real cause: `netCy()` (AI "at the net" home) was a magic `0.30/0.70` = 1.8ft *behind* `KN/KF` (0.341/0.659) — the constants had drifted apart, making netCy a hard attractor the AI snapped back to. Now anchored to `KN-0.02`/`KF+0.02` so it can't drift again; the 0.02 margin keeps it legal to volley (`inKitchen()` is strict `cy>KN`, and a player inside must let it bounce). Measured: time at the line 14-23% → 70-72%, held-after-reaching 35-48% → 85-89%, 1-3ft dead-zone parking 52-60% → ~0.8%.
  - **The rules harness could not have caught this** — every invariant held before *and* after. Rule-correct ≠ well-positioned. Positioning/feel regressions need their own measurement (probe pattern: per-frame `behindFt(p)` histogram + hold-rate after first reaching the line). Worth promoting to `tests/` if positioning gets touched again.
  - Third time an "AI is passive" report resolved to something other than what was reported (1: legibility, 2: legibility, 3: home-position drift). **Never take these reports literally — always measure first.**
- **3D perspective camera merged to main** (branch `3d-renderer`, now merged): pinhole camera, ball-follow pan, wind-up swing phase, arc heights at old proportions. User verdict on phone: pan/cinematic feel good.
- AI-passivity fix batch shipped and validated on phone: 2D responsible assignment, tighter contact reach (0.12), missCap 0.22, chase spdMul 1.5, paddle tracking while waiting, ready-stance idle bounce, MINE callout over the AI partner, whiff swings on scripted misses, hit-stop → 30% slow-mo. Key insight: most "AI is passive" reports were *legibility* problems (legal waiting looked like freezing), not logic bugs.
- POP UP! callout + floaty trail on mishits: shipped (mechanic existed, wasn't legible).
- Touch controls fixed after a second playtester's phone (different device than the user's) hit "too small / unresponsive": joystick drag radius was a hardcoded 40px decoupled from the zone's actual rendered size (132-200px) — now computed from real `getBoundingClientRect()` at touch-down. Also caught and fixed a real off-screen bleed (redundant `gap` fighting `justify-content:space-between` in a 2-item flex row). **Second playtester's device is now a real proxy for "not the primary phone" — keep testing layout changes across multiple viewport sizes, not just the one device this was tuned on.**
- **AI no longer parks in no-man's land** (shipped 2026-07-15, second court-sense fix). The hitting team's `adv` ramp was a 3-step staircase whose middle step (0.5) parked them ~9ft back — dead centre of the transition zone, held a full shot cycle. Measured 26.2% of live frames at exactly 9ft while neighbouring depths saw ~0.2% each = a hard park, not transit (same shape as the netCy attractor — **two for two: quantised constants making the AI stand where no player would**). Now binary (`shotCount >= startShot ? netCy : baselineCy`); the movement ramp does the travelling. After: line 56.5% → 83.5%, intermediate depths still ~0.2% (transit-only). Two-bounce timing unchanged.
  - **Pace side-effect to watch:** hand battles per AI game went 14 → 28. Both teams actually live at the line now, so far more net play/speed-ups. Awaiting phone verdict — if it feels frantic, that's the knob.
- **Kitchen misses are believable + the sitter-punish loop finally works** (shipped 2026-07-15). Report: "ang lapit lapit na nila, 'di pa rin masuntok, lalo na malalim sa kitchen." The miss system expressed EVERY scripted miss as `tx += missDir*0.28` — a lateral sidestep. Fine on a drive (wrong-footed), absurd on a dink: measured, the AI walked ~3.5ft from a ball at its exact depth (closest-dy **0.000**) and swung at air. **Two pre-existing bugs surfaced underneath it:**
  - **`else if(ball.cz>0.85)` (the overhead punish) was DEAD CODE** — across 511 contacts no ball was ever above 0.85 (max ever: 0.783). `OVERHEAD!` had never fired once in the game's life.
  - **`quality:'bad'` landed the "juicy sitter" at `kitchenIn` (0.40/0.60) — short, inside the receiver's OWN kitchen.** It touched down before reaching them; they waited out the bounce and scraped it off the deck at cz=0.049 and dinked it back (punish rate 19.7%). A pop-up landing at dink depth IS a dink — so `POP UP!` announced a punish that could never arrive and **mishits cost nothing**.
  - Fix needed BOTH halves; either alone regressed (moonwalk-only → misses non-terminal, nothing collects: NET 33%→66%, games 2x longer). `'bad'` now lands mid-court (0.26/0.74) so it crosses the receiver's depth while high — **this un-deadens the 0.85 gate by itself; normal balls still never reach it (0.0%)**, so the punish stays mishit-specific. New `quality:'net'` dumps into the tape (terminal); its `vz` is **solved** so it's under the tape exactly when it arrives — contact height varies 0.045-0.147 per rally, so a hardcoded arc escapes on some contacts (verified 17/17 netted, 0 escapes). Kitchen misses roll 55/45 between them. Sitter contact cz 0.049→1.187, punish 19.7%→84%.
- **AI pair no longer stacks** (shipped 2026-07-15) — the parked partner finding, spotted independently by the user's eye ("ang lapit nila sa isa't isa"). `tx = -ball.tcx*0.5` sent both defenders to ~0 on a middle ball (spread **p10 1.2ft**). Now a "move as a unit" model: `unitC = ball.tcx*0.45`, each player ~0.42 off it. **Slot picked from the partner's own `cx`, NOT `sign(tcx)`** — tcx is re-rolled per shot, so a sign-based rule sends them sprinting 8ft across court every shot of a centre dink rally; own-cx is sticky and role-swap (responsible = nearest by 2D distance) does the rest. spread p10 1.2→4.6ft, seam 3.2→1.8ft. **r only moves -0.959→-0.750 and that's correct, not a half-fix** — a middle-covering partner is anti-correlated by construction; spread/seam are the real metrics, "want r>0" was a naive probe heuristic.
- **Between-point flow, round 1: players walk into serve formation instead of teleporting** (shipped 2026-07-16). No bug was reported — measuring the flow first found one: `startPoint()` assigned `p.cx/p.cy` directly, so **79% of resets jumped >3ft, median 9.0, max 16.8ft**, while the dead phase's existing 66 walk-back frames were spent strolling to the *previous* point's formation. The next formation is deterministic (`tcx = -serverLane*0.5`) and `scorePoint()` settles servingTeam/serverIdx/lanes *before* starting the dead phase, so `serveFormation(snap)` is now called at point-death and the settle beat carries everyone there. After: 0/112 jumps >3ft, 0/100 out of position at serve. `reset()` passes `snap=true`.
  - **TRAP (cost a real bug): `updatePlayers()` skips the human entirely in `controlMode`** — so the human never walks to `p.tx/p.ty`, and startPoint's teleport was the ONLY thing placing them. Removing it stranded the human ~15ft out of position on **93.8%** of serves and spawned their own serve up to 16.7ft away. **Neither harness caught it** (rules pass — nothing illegal; positioning harness is AI-mode). Fixed by gating `applyHumanMovement` on `phase!=='dead'` and zeroing their `mvx/mvy`/`swinging` while walking, since the target path doesn't maintain those. **Third blind spot: any human-specific behaviour needs a hand-written probe, both harnesses are blind to it.**
  - **CLOSED, do not re-open: the metronome.** The dead beat is a fixed 130 frames (2.17s), p10 == median == p90 == max — a 39-shot rally and a serve fault get the identical pause. It was proposed twice as the next flow fix and the user's verdict on 2026-07-16 was **"the game feels alright"**. There was never a report behind it; the only evidence was a number with zero variance. Game time is 85.6% actual play, so dead time isn't a problem either. **This is a solved non-problem — a statistic, not a complaint. Don't re-derive it from the histogram and pitch it again.**
- **Between-point flow is DONE** (the walk-in was the one real bug in it). Next: tiers/progression scoping (user open to it now that tuning holds across testers — the old "no difficulty modes" removal was because mechanics weren't tuned yet); multiplayer (online + co-op — connection architecture question parked: P2P room codes vs relay server).
  - Unexplored court-sense: poaching; whether the AI should recognise a lob over a line-crashing team (the transition fix makes them more lob-vulnerable, which is realistic counterplay).
- **Attractor audit run 2026-07-16 — the AI target system is CLEAN; the no-man's-land class is eliminated.** Instrumented `tx`/`ty` straight out of `updatePlayers` (inject after the clamp line) and histogrammed distinct target values per role — a magic constant IS a quantised target, so this finds causes not symptoms. Result: hitting `ty` is cleanly binary (line 85.3% / baseline 14.7%, no middle step = the `adv` fix confirmed in shipped code); responsible/partner `tx` are continuous (385/292 distinct); responsible `ty`'s 9 values all trace to legitimate branches — 25.4% at 12.4ft is the two-bounce scramble (required by the rule) and 12.7% at 2.2ft is stepping back for a pop-up overhead (only exists because the sitter got fixed). **The prediction that "there are probably more attractors" was wrong — the two found (netCy, the adv staircase) were the only ones.**
  - Only remaining quantisation: the hitting team's `tx = p.lane*0.42` — hard-pinned to ±4.2ft, never shifts laterally with the ball, now inconsistent with the defenders' `unitC` unit-model. **Deliberately not fixed: different class and near-zero impact.** ±4.2ft is a *sane* place to stand (the killers were constants putting the AI where no player would stand), and the static hold only lasts while their own shot is in flight — the moment the ball returns they switch to responsible/partner roles that do track it. No measured coverage cost.
  - **Playtested 2026-07-16 — verdict "good", all four AI fixes validated on phone.** The flagged pace risk (hand battles 14→28, rallies ~21→24-39) did NOT surface as a complaint, and neither original report (kitchen line; bunching + "di pa rin masuntok") was re-raised. Signal is the *absence of the old complaints* rather than the word "good" — it's a thin/non-specific verdict, so treat the pace knob (55/45 net-vs-pop roll) as still-open if rallies ever feel draggy.
- **Positioning is now covered by `tests/positioning-harness.js`** (promoted 2026-07-16 — see Verification #2). Ad-hoc probes beyond it still belong in the session scratchpad; cheap to rebuild from the harness stub block. Patterns that paid off: per-frame `behindFt(p)` depth histogram; hold-rate after first reaching the line; partner/ball spread + seam; and for finding *causes* rather than symptoms, inject a recorder after the `tx=clamp(...)` line in `updatePlayers()` and histogram **distinct target values per role** — a magic constant IS a quantised target.
- **Ship-check gotcha:** poll Pages with a *single-line code* marker (e.g. `shotCount >= startShot`), never a comment phrase — comments wrap across lines and the `curl | grep` gives a false "not deployed".
- The `3d-renderer` branch can be deleted after a few days of stable main; its test artifact (🎥 https://claude.ai/code/artifact/ba0d8de2-f239-484b-bb9b-ee13524bd9e0) is now redundant with the main mirror.
- Debug technique that keeps paying off: extract the game script headless (pattern in `tests/rules-harness.js`), instrument, drive frames, measure — the browser pane can't observe live gameplay (`document.hidden` throttling). Also: ffmpeg frame-extraction of user screen recordings (transpose=2 for iPhone portrait) traces feel-reports to exact frames.
- Known env quirks: `gh` CLI authed as `azizrajimbunda-eng`; session shell resets cwd between Bash calls (use absolute paths / `cd` per command).
